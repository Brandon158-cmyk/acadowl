/**
 * Re-export cn utility from the standard location.
 * shadcn/ui generates this at src/lib/utils.ts — this file at the
 * sprint-specified path is kept for discoverability.
 */
export { cn } from '@/lib/utils';
